# HR Agent Demo — 全局重构方案

> 写给自己的提示词。执行时逐条对照，不遗漏。

---

## 一、产品总监视角：这个 Demo 要讲什么故事？

### 30 秒规则
面试官打开页面，30 秒内必须完成以下判断：
1. 这个人**懂 AI 产品** — 不是贴了 AI 标签的表单，是真的在用 Agent 能力
2. 这个人**懂用户** — 交互设计不是拍脑袋，有依据
3. 这个人**能落地** — 代码跑得通，不是 PPT

### 核心叙事
**"一个会追问的 AI 招聘助手"** — 不是生成 JD 的工具，而是帮你理清需求的对话伙伴。

关键差异化：市面上的 JD 生成器是"输入岗位名 → 输出 JD"。我们做的是"你说前端工程师，我问你做什么类型的项目、用什么技术栈、团队多大，然后帮你生成"。这才是 AI 产品的正确姿态。

### 面试场景映射
| 面试官角色 | 他们会看什么 | 我们展示什么 |
|-----------|-------------|-------------|
| 产品总监 | 产品思维、用户洞察 | Agent 主动追问、选项引导、结构化输出 |
| CTO | 技术架构、模型选型 | ReAct 循环、工具调用、SSE 流式、MiMo 模型 |
| HR | 业务理解、场景还原 | 真实的 HR 需求分析流程、JD 质量校验 |
| 交互设计师 | 交互逻辑、认知负担 | ThoughtChain 可视化、A2UI 结构化卡片、快速选项 |
| 视觉设计师 | 视觉层次、品牌调性 | 暗色主题、一致的色彩系统、排版节奏 |

---

## 二、CTO 视角：技术架构决策

### 已验证的决策（保留）
- MiMo v2.5 Pro 模型（Token Plan）
- ReAct Agent 循环（Thought → Action → Observe → Finish）
- SSE 流式推送
- Next.js 14 + Vercel 部署

### 已踩的坑（必须避免）
| 问题 | 根因 | 解决方案 |
|------|------|---------|
| 白底白字 | createStyles 的 token.xxx 在客户端取到浅色默认值 | **全部用明确暗色值，不用 token** |
| CSS 语法错误 | 编辑时误合并两行（font-size: 11max-width） | **CSS 用模板字符串单行写法，不拼接** |
| network error | JS 解析 CSS 失败 → 组件不渲染 → fetch 超时 | **CSS 写完必须 npm run build 验证** |
| "请补充详细信息" | Agent prompt 没有约束追问方式 | **prompt 中明确要求具体问题+选项** |
| AnalysisCard 显示 "缺失: 详情" | fallback 数据太简陋 | **LLM 返回的 analysis 必须包含 questions 数组** |
| XProvider 不生效 | antd-style 的 createStyles 读 ConfigProvider 而非 XProvider | **双层嵌套：ConfigProvider + XProvider** |

### 前端技术栈
```javascript
// 正确的 Provider 嵌套（已验证）
<ConfigProvider theme={{ algorithm: antdTheme.darkAlgorithm }}>
  <XProvider>
    {/* 组件 */}
  </XProvider>
</ConfigProvider>

// 正确的样式写法（不用 token）
const useStyles = createStyles(({ css }) => ({
  container: css`
    background: #000;
    color: rgba(255, 255, 255, 0.88);
    border: 1px solid rgba(255, 255, 255, 0.06);
  `,
}));
```

---

## 三、HR 视角：真实业务流程

### HR 日常招聘痛点
1. **需求模糊** — 业务方说"我要个前端"，但不清楚具体做什么
2. **JD 质量参差** — 写出来的 JD 不够吸引人、要求不清晰
3. **反复沟通** — 来回改 JD 浪费时间
4. **标准不一** — 不同 HR 写的 JD 风格差异大

### Agent 应该模拟的 HR 工作流
```
Step 1: 接收需求（"前端工程师"）
Step 2: 分析已知信息（岗位名称）
Step 3: 识别缺失信息（项目类型、技术栈、团队、薪资）
Step 4: 主动追问（具体问题 + 选项，降低沟通成本）
Step 5: 生成 JD（结构化、专业）
Step 6: 质量校验（覆盖率、具体性、改进建议）
```

### Agent 追问的设计原则（来自用户研究访谈知识）
1. **开放式 + 封闭式结合** — 先给选项快速选择，再留空间补充
2. **一次只问 2-3 个问题** — 降低认知负担，不要一次问 5 个
3. **问题要具体** — 不问"还有什么要求？"，问"技术栈是 React 还是 Vue？"
4. **选项要覆盖 80% 场景** — 用常见选项 + "其他"兜底
5. **追问有层次** — 先问核心（项目类型、技术栈），再问细节（薪资、福利）

---

## 四、交互设计师视角：A2UI 组件设计

### 组件职责划分
| 组件 | 作用 | 什么时候出现 |
|------|------|-------------|
| Welcome | 第一印象、引导 | 页面加载，无消息时 |
| Prompts | 快速启动 | 页面加载，无消息时 |
| ThoughtChain | Agent 思维可视化 | Agent 执行过程中 |
| Bubble.List | 对话历史 | 有消息后 |
| Think | 正在思考提示 | Agent 处理中 |
| Sender | 用户输入 | 始终在底部 |

### A2UI 结构化卡片设计

#### AnalysisCard（需求分析卡片）
**作用：** 引导用户补充信息，不是警告
**设计：**
```
┌─────────────────────────────────────────┐
│ 📋 需求分析                              │
│                                         │
│ ✅ 已理解                                │
│    [岗位名称: 前端工程师]                 │
│                                         │
│ ❓ 我还需要了解（请回答以下问题）          │
│                                         │
│ 1. 这个岗位主要负责什么类型的项目？        │
│    ┌──────────┐ ┌──────────┐            │
│    │ B端后台   │ │ C端H5    │            │
│    └──────────┘ └──────────┘            │
│    ┌──────────┐ ┌──────────┐            │
│    │ 中后台    │ │ 移动端App │            │
│    └──────────┘ └──────────┘            │
│                                         │
│ 2. 技术栈是什么？                        │
│    ┌──────────┐ ┌──────────┐            │
│    │ React+TS │ │ Vue+TS   │            │
│    └──────────┘ └──────────┘            │
│    ┌──────────┐                          │
│    │ 其他      │                          │
│    └──────────┘                          │
└─────────────────────────────────────────┘
```

**关键交互：**
- 选项按钮可点击 → 直接发送答案
- 点击后该问题的选项变灰（已回答）
- 2-3 个问题，不是 5 个

#### JDCard（JD 展示卡片）
**作用：** 结构化展示生成的 JD
**设计：**
```
┌─────────────────────────────────────────┐
│ 前端工程师 · B端SaaS · 25K-40K          │
│ ─────────────────────────────────────── │
│ 📝 岗位概述                              │
│ 负责公司核心产品的前端开发...              │
│                                         │
│ 🎯 核心职责                              │
│ • 负责订单管理模块的开发和维护             │
│ • 参与前端架构设计和技术选型               │
│ • ...                                   │
│                                         │
│ 📋 任职要求                              │
│ [必须] 3年以上 React 开发经验             │
│ [必须] 熟练使用 TypeScript                │
│ [优先] 有 B端SaaS 产品经验               │
│ [加分] 有 Ant Design 使用经验            │
│                                         │
│ 🎁 福利待遇                              │
│ [14薪] [弹性工作] [技术氛围好]            │
└─────────────────────────────────────────┘
```

#### ValidationPanel（质量校验）
**作用：** 展示 JD 质量评估结果
**设计：**
```
┌─────────────────────────────────────────┐
│ 🔍 质量校验              85 分           │
│                                         │
│ ✅ 岗位职责清晰                          │
│ ✅ 任职要求具体                          │
│ ⚠️ 薪资范围可更精确                      │
│    建议明确是税前还是税后                 │
│                                         │
│ 💡 改进建议                              │
│ • 建议增加团队规模信息                    │
│ • 建议补充汇报关系                        │
└─────────────────────────────────────────┘
```

### 交互流程图
```
[Welcome + Prompts]
      │
      ▼
  用户输入 "前端工程师"
      │
      ▼
  [ThoughtChain: Think → analyze_requirements]
      │
      ▼
  [Bubble: AnalysisCard] ← 结构化分析 + 选项按钮
      │
      ▼
  用户点击 "B端后台" + "React+TS"
      │
      ▼
  [ThoughtChain: Think → generate_jd]
      │
      ▼
  [Bubble: JDCard] ← 结构化 JD
      │
      ▼
  [ThoughtChain: Think → validate_jd]
      │
      ▼
  [Bubble: ValidationPanel] ← 质量校验
      │
      ▼
  [Meta: 模型 · 步数 · tokens · 耗时]
```

---

## 五、视觉设计师视角：暗色主题色彩系统

### 色彩 Token（全部明确值，不用 antd token）

| 用途 | 色值 | 说明 |
|------|------|------|
| 背景-主 | `#000000` | 纯黑 |
| 背景-容器 | `#0a0a0a` | Header/Footer/Sender |
| 背景-卡片 | `#141414` | 气泡/卡片/Welcome |
| 背景-悬浮 | `#1a1a1a` | hover 状态 |
| 文字-主 | `rgba(255,255,255,0.88)` | 标题/正文 |
| 文字-次 | `rgba(255,255,255,0.65)` | 描述/次要信息 |
| 文字-辅 | `rgba(255,255,255,0.45)` | 标签/状态 |
| 文字-弱 | `rgba(255,255,255,0.3)` | 页脚/模型名 |
| 边框 | `rgba(255,255,255,0.06)` | 分割线/边框 |
| 边框-强 | `rgba(255,255,255,0.08)` | 卡片边框 |
| 主色-蓝 | `#3370ff` | 按钮/链接/标签 |
| 主色-蓝-bg | `rgba(49,109,255,0.12)` | 蓝色标签背景 |
| 成功-绿 | `#00b578` | 状态点/通过 |
| 警告-黄 | `#f59e0b` | 警告/缺失 |
| 错误-红 | `#ef4444` | 错误/必须 |

### 排版系统
| 元素 | 字号 | 字重 | 颜色 |
|------|------|------|------|
| Logo | 15px | 600 | 0.88 |
| 标签 | 10px | 600 | #3370ff |
| 状态 | 12px | 400 | 0.45 |
| 模型名 | 11px | 400 | 0.3 |
| 区域标题 | 11px | 600 | 0.3 |
| 卡片标题 | 18px | 600 | 0.95 |
| 正文 | 13-14px | 400 | 0.75 |
| 选项按钮 | 12px | 400 | #3370ff |
| 页脚 | 11px | 400 | 0.3 |

### 间距系统（8px 网格）
- 容器内边距：20px
- 卡片内边距：16-20px
- 元素间距：8-12px
- 区域间距：16-20px

---

## 六、执行清单

### 前端重构
- [ ] 重写 styles（全部明确暗色值）
- [ ] 重写 AnalysisCard（引导式，不是警告式）
- [ ] 重写 JDCard（结构化展示）
- [ ] 重写 ValidationPanel（评分 + 检查项 + 建议）
- [ ] 选项按钮点击 → 发送答案
- [ ] Bubble role 配置（contentRender + footer）
- [ ] 错误处理（超时 + 重试）

### 后端重构
- [ ] 优化 system prompt（主动追问 + 选项）
- [ ] analyze_requirements 返回 questions 数组
- [ ] generate_jd 返回结构化 JD JSON
- [ ] validate_jd 返回结构化校验 JSON
- [ ] 错误处理（429 重试 + 超时）

### 验证
- [ ] npm run build 无报错
- [ ] Vercel 部署成功
- [ ] 暗色主题正确（无白底白字）
- [ ] 选项按钮可点击
- [ ] ThoughtChain 正常显示
- [ ] 移动端适配正常
